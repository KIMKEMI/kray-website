# 이 워크플로우는 로컬 PC에서 실행하는 방식으로 변경되었습니다.
# python update_stats.py 를 로컬에서 실행 후 GitHub Desktop으로 Push하세요.
name: Update Instagram Follower Count

on:
  workflow_dispatch:  # 수동 실행만 (자동 스케줄 비활성화)

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "로컬 PC에서 update_stats.py 를 실행 후 Push해주세요."
